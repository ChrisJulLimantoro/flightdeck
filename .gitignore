# dependencies
node_modules/

# build output
dist/
build/
web/dist/
# the SPA, copied in by `pnpm build` — shipped in the npm tarball, not in git
server/public/
.vite/

# logs
*.log
npm-debug.log*

# secrets — app state lives in ~/.flightdeck, but never allow a stray one
.env
.env.*
!.env.example

# editors / OS
.DS_Store
.vscode/
.idea/
*.swp
